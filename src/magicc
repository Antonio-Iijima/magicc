#!/bin/bash


clear

python3 main.py compile $1 $3

python3 main.py lock
pyinstaller main.py \
    --onefile \
    --name=$2 \
    --add-data "config.json:."
python3 main.py unlock

rm -r build
rm ./*.spec
